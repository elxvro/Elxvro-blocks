name: ELXVRO Blocks APK Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter 3.47.4
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.4'
          channel: stable
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Check ZIP
        run: |
          test -f elxvro-blocks-v0.3.0.zip
          unzip -t elxvro-blocks-v0.3.0.zip

      - name: Extract project
        run: |
          rm -rf extracted
          mkdir extracted
          unzip -q elxvro-blocks-v0.3.0.zip -d extracted
          ls -la extracted/elxvro-blocks-v0.3.0

      - name: Generate Android platform
        working-directory: extracted/elxvro-blocks-v0.3.0
        run: |
          flutter create \
            --platforms=android \
            --org com.elxvro \
            --project-name elxvro_blocks \
            .

      - name: Install dependencies
        working-directory: extracted/elxvro-blocks-v0.3.0
        run: flutter pub get

      - name: Analyze
        working-directory: extracted/elxvro-blocks-v0.3.0
        run: flutter analyze

      - name: Test
        working-directory: extracted/elxvro-blocks-v0.3.0
        run: flutter test

      - name: Build release APK
        working-directory: extracted/elxvro-blocks-v0.3.0
        run: flutter build apk --release

      - name: Verify APK
        run: |
          test -f "${{ github.workspace }}/extracted/elxvro-blocks-v0.3.0/build/app/outputs/flutter-apk/app-release.apk"
          ls -lh "${{ github.workspace }}/extracted/elx
